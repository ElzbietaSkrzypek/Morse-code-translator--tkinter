# Morse-code-translator
### It is a program to translate "normal" text to morse code and also in reverse mode, from morse code to "normal" text.

1. Clone repo (both py files necessary)
2. Include morse-code.png for UI setup
3. Run 'main.py'

## Requirements
pip install -r requirements.txt
